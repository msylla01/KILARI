import { FilterplanPipe } from './filterplan.pipe';

describe('FilterplanPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterplanPipe();
    expect(pipe).toBeTruthy();
  });
});
